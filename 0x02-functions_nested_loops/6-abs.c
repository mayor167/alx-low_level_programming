#include "main.h"
/**
 * _abs -A program that computes absolute value of an integer
 *@num: The character to be checked
 *Return: - num, otherwise num
 */
int _abs(int num)
{
	if (num < 0)
		return (-num);
	else
		return (num);
}
