#include "main.h"
#include <stdio.h>
/**
 *main - A program that prints program name
 *@argc: number of command line
 *@argv: array of name
 *Return: 0 (Success)
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
